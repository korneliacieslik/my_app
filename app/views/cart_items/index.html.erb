<p id="notice"><%= notice %></p>

<h1>Cart Items</h1>

<table>
  <thead>
    <tr>
      <th>Product</th>
      <th colspan="3"></th>
    </tr>
  </thead>
    <% if !session[:cart].nil? %>
      <%session[:cart].each do |product|  %>
        <%= product %>
        <br>
      <% end %>
    <% end %> 
  </table>

<%= link_to 'Back', products_path %>
<br>
