<!DOCTYPE html>
<html>
  <head>
    <title><%=full_title(yield(:title))%></title>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!--
    The effect of this line is to look for a file called app/views/layouts/_shim.html.erb, evaluate its contents, and insert the results into the view.
    -->
    <%= render 'layouts/shim' %>
    <%= debug(params) if Rails.env.development? %>
  </head>

  <body>
    <!--
    The effect of this line is to look for a file called app/views/layouts/_header.html.erb, evaluate its contents, and insert the results into the view.
    -->
    <%= render 'layouts/header' %>
    <%= yield %>
    <%= render 'layouts/footer' %>
  </body>
</html>
